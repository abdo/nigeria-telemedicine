import { View, Text } from 'react-native';
import React from 'react';

import styles from './styles';

const NewSubmissionScreen = () => (
  <View style={styles.container}>
    <Text>New Submissions</Text>
  </View>
);

export default NewSubmissionScreen;
